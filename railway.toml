[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn agent_api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
